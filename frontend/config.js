/* SmartStreet frontend config.
 *
 * Leave empty for local development or a single-service deploy (the FastAPI
 * backend serves this frontend from the same origin).
 *
 * For a SPLIT deploy (this frontend on Vercel, backend on Render/Railway/Fly),
 * set this to your backend's public URL, e.g.:
 *   window.SMARTSTREET_API = "https://smartstreet-api.onrender.com";
 */
window.SMARTSTREET_API = "";
